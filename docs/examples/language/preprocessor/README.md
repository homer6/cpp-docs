# Preprocessor

Text processing that runs before compilation. Mirrors
[cppreference's preprocessor](https://en.cppreference.com/w/cpp/preprocessor).

| Page | What |
|---|---|
| [Preprocessor](preprocessor.md) | `#include`, `#define`, conditional compilation, and what to use instead |

> Modern C++ replaces most preprocessor uses with language features
> (`constexpr`, `enum`, templates, [modules](../modules/README.md)). This page is
> as much about *avoiding* the preprocessor as using it.
